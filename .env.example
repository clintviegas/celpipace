# CELPIPACE — environment template
# Copy to .env.local and fill in values. Never commit .env.local.

# ── Public (safe for Vite bundle — VITE_ prefix) ───────────────────────────
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
VITE_CDN_URL=https://pub-0a4aab7639024035a20be5b348e78b40.r2.dev
VITE_SITE_URL=https://www.celpipace.ca

# ── Server-only (API routes / scripts — never VITE_ prefix) ──────────────────
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
OPENAI_API_KEY=sk-...

# Direct Postgres URI — optional; enables scripts/apply-coach-patch.mjs auto-apply
# Supabase → Project Settings → Database → Connection string → URI
# SUPABASE_DB_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Stripe
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
STRIPE_PRICE_WEEKLY=price_...
STRIPE_PRICE_MONTHLY=price_...
STRIPE_PRICE_ANNUAL=price_...
STRIPE_PROMOTION_CODE_CELPIP25=promo_...

# Site / auth redirects
PUBLIC_SITE_URL=https://www.celpipace.ca

# Email (Brevo)
BREVO_API_KEY=
BREVO_LIST_ID=
BREVO_LIST_PREMIUM=
BREVO_LIST_CANCELLED=
EMAIL_FROM=hello@celpipace.ca

# Vercel cron protection
CRON_SECRET=

# Optional: GSC pull scripts (local only — see docs/GSC_INDEXING_CHECKLIST.md)
